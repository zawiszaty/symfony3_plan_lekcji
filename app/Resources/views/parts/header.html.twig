<header>
    logo
    {% if is_granted('ROLE_USER') %}
        <a href="{{ logout_path('main') }}">Logout</a>
    {% else %}


        <a href="{{ path('fos_user_security_login') }}">Login</a>
    {% endif %}

</header>